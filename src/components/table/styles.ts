import styled from 'styled-components';
import MaterialReactTable from 'material-react-table';

export const TableContainer = styled(MaterialReactTable)`
	* {
		background-color: transparent !important;
	}
`;
